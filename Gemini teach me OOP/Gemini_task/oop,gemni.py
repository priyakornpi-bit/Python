def heal(amount):
    # แทนที่จะปริ้น เราส่งค่ากลับไปให้คนเรียก
    return amount

# เวลาเรียกใช้ ต้องเอา "ถุงมารองรับของ" (ตัวแปร)
my_heal = heal(100)

print(f"ฉันได้รับเลือดกลับมา: {my_heal}")
# ที่ต้องทำแบบนี้ เพราะในอนาคตเราจะเขียนว่า: current_hp = current_hp + heal(100)